<div class="max-w-sm sm:max-w-xl mx-auto py-8 px-4">
    {{#each races}}
        <a href="/race/{{id}}">
            <div class="bg-white rounded-2xl shadow mb-2 overflow-hidden hover:scale-105 transition-all">
                <div class="flex justify-between items-center gap-4 px-2 sm:px-6 py-1 border-b">
                    <div class="flex items-center gap-4">
                        <div class="flex justify-center">
                            <img src="circuits/{{track_img}}" alt="Track for {{name}}" class="max-h-14 sm:max-h-20 rounded" />
                        </div>
                        <div>
                            <img src="flags/{{flag}}" class="black-border inline h-4 sm:h-6 mr-2 sm:mr-4" alt="flag={{flag}}">
                            <span class="font-semibold sm:text-lg flex-1">{{name}}</span>
                        </div>
                    </div>
                    <div class="hidden sm:block">
                        <span class="text-sm text-gray-500">{{formattedDate}}</span>
                    </div>
                </div>
            </div>
            <div class="flex justify-center mb-2 text-gray-400"><span>↓</span></div>
        </a>
    {{/each}}
    <div class="flex justify-center mb-2 text-gray-400">...</div>
</div>
